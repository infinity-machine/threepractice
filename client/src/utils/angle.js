// export const angleToRadians = (angleInDeg) => (Math.PI / 180) * angleInDeg;

export default function angleToRadians(angleInDeg) {
    const angleInRadians = ((Math.PI / 180) * angleInDeg)
    return angleInRadians
}